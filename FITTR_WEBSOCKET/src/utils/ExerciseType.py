import pandas as pd
class ExerciseType:
    SQUATS = "SQUAT"
    
    SQUATS_THRESHOLD = 0.5 # when a person's left/right knee goes below this threshold a rep is done
    
    
    BICEP_CURLS = "BICEP_CURLS"